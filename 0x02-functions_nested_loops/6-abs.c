#include "main.h"

/**
 * _abs -checks a number\'s sign.
 * @c: value of of in.
 *
 * Return: n itself.
 */

int _abs(int c)
{
	if (c = 0)
	{
		return (0);
	}
/*	else if (c < 0)
	{
		_putchar('-');
		return (-1);
	}*/
	else
	{
		return (n);
	}
}
